package frame

/*
import (
	"github.com/as/etch"
	"testing"
)
func TestInsertOneChar(t *testing.T) {
	h, _, have, _ := abtestbg(R)
	h.Insert([]byte("1"), 0)
	h.Untick()
	//etch.WriteFile(t, `testdata/TestInsertOneChar.expected.png`, have)
	etch.AssertFile(t, have, `testdata/TestInsertOneChar.expected.png`, "TestInsertOneChar.png")
}

func TestInsert10Chars(t *testing.T) {
	h, _, have, _ := abtestbg(R)
	for i := 0; i < 10; i++ {
		h.Insert([]byte("1"), 0)
	}
	//etch.WriteFile(t, `testdata/TestInsert10Chars.expected.png`, have)
	etch.AssertFile(t, have, `testdata/TestInsert10Chars.expected.png`, "TestInsert10Chars.png")
}

func TestInsert22Chars2Lines(t *testing.T) {
	h, _, have, _ := abtestbg(R)
	for j := 0; j < 2; j++ {
		for i := 0; i < 10; i++ {
			h.Insert([]byte("1"), h.Len())
		}
		h.Insert([]byte("\n"), h.Len())
	}
	// etch.WriteFile(t, `testdata/TestInsert22Chars2Lines.expected.png`, have)
	etch.AssertFile(t, have, `testdata/TestInsert22Chars2Lines.expected.png`, "TestInsert10Chars.png")
}
*/